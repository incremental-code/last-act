{
  "name": "zero",
  "version": "0.1.0",
  "type": "module",
  "description": "A minimal signal-based UI framework",
  "scripts": {
    "test": "node zero.test.js",
    "test:patterns": "node patterns.test.js",
    "test:all": "npm run test && npm run test:patterns",
    "build": "node build.js",
    "build:docs": "node build-docs.js",
    "build:all": "npm run build && npm run build:docs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jsdom": "^24.0.0"
  },
  "dependencies": {
    "signal-polyfill": "^0.2.2"
  }
}
